<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileDuckLibras"
        tools:targetApi="31">

        <activity
            android:name=".Home.Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AulasActivity" />
        <activity android:name=".Login.presetation.Login" />
        <activity android:name=".Anotacao.AnotacaoActivity" />
        <activity android:name=".Traducao.TraducaoActivity" />
        <activity android:name="com.example.mobileducklibras.FlashCard.presetation.FlashcardActivity" />
        <activity android:name="com.example.mobileducklibras.user.presetation.ProfileActivity" />
        <activity android:name="com.example.mobileducklibras.Cadastro.presetation.Cadastro" />
        <activity android:name="com.example.mobileducklibras.Dicionario.presetation.DicionarioActivity" />
    </application>

</manifest>
